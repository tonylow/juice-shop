import { RequestHandler } from 'express';
export declare const TEST_HOOKS_FILE_NAME: string;
export default class TestHooksMiddleware {
    private constructor();
    private static _instance;
    static readonly instance: TestHooksMiddleware;
    currentTestHooks: string;
    handler: RequestHandler;
}
